apiVersion: v1
kind: ConfigMap
metadata:
  name: env-vars-{{.Name}}
  namespace: {{.Namespace}}
  labels:
    app: devenv-{{.Name}}
data:
  USER: "{{.Name}}"
  UID: "{{.GetUserID}}"
  IS_ADMIN: "{{.IsAdmin}}"
  GIT_NAME: "{{.Git.Name}}"
  GIT_EMAIL: "{{.Git.Email}}"
